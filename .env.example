# Em dev com Vite, use o proxy: base relativa
VITE_API_URL=/api/v1
# Backend no Docker (FlowCred-Back docker-compose): API exposta em localhost:8001
VITE_PROXY_TARGET=http://localhost:8001
# Se rodar uvicorn na máquina na porta 8000, use:
# VITE_PROXY_TARGET=http://localhost:8000
